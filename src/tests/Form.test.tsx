import App from '@/App';
import { render } from '@testing-library/react';

describe('SchemaForm Component testing', () => {
    it('SchemaForm is rendering', () => {
        render(
            <App />
        );
    });
});
